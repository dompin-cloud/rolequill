# Pinned to known-good versions so a fresh install on the host can't pull a
# breaking new release. Update deliberately, then redeploy.
Flask==3.1.3
Werkzeug==3.1.8
Jinja2==3.1.6
requests==2.34.2
openpyxl==3.1.5
python-docx==1.2.0
pypdf==6.13.3
# optional: only needed if you launch via `flask run` instead of `python run.py`.
# RoleQuill loads .env itself, but this also lets the Flask CLI load it (silences
# the "Install python-dotenv" tip).
python-dotenv==1.2.2

# --- production ---
stripe>=9.0,<13.0                        # live payments (webhook + checkout)
waitress>=3.0,<4.0                       # cross-platform production WSGI server
gunicorn>=21.0,<24.0 ; sys_platform != "win32"  # Linux production WSGI server
